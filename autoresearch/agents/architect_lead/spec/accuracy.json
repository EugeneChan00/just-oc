{
  "schema_version": "1.0",
  "category": "accuracy",
  "description": "Evaluates whether the architect_lead produces factually correct architecture deltas, logically sound module/interface decisions, and instruction-compliant System Slice Architecture Briefs that faithfully translate approved strategic slices into minimal coherent architectural moves.",
  "metrics": {
    "factual_correctness": {
      "sub_metrics": {
        "fact_accuracy": {
          "description": "Architect lead produces architecture deltas grounded in the actual codebase, real dependency graphs, actual module boundaries, and true runtime constraints. Claims about existing interfaces, state ownership, control flow, and integration points are verifiable against the repository. The lead does not hallucinate modules that do not exist, invent dependencies between components that have no relationship, or assert integration capabilities that the current stack does not support.",
          "expected_outcome": "Every factual claim in the Architecture Brief — module names, file paths, dependency relationships, interface contracts, runtime constraints, framework capabilities — is verifiable against the actual codebase and system context. No invented components, no fabricated integration points, no hallucinated API surfaces. Worker dispatch briefs reference real files and real modules that exist in the repository, and the recommended architecture delta operates on the actual system topology rather than an imagined one.",
          "observable_behavior": "Agent references specific files, modules, and interfaces that exist in the repository when building the system context model. Architecture delta descriptions match actual system structure and real dependency relationships rather than assumed or invented ones. Worker dispatch briefs include repository inspection scopes that point to real paths. No claims appear that contradict the existing codebase when cross-referenced, and all stated runtime constraints correspond to actual platform or framework limitations.",
          "output": null
        },
        "source_citation": {
          "description": "Architect lead traces architectural decisions to their authoritative sources: the upstream Strategic Slice Brief, CEO directives, existing codebase structure, worker analysis returns, ranked architecture drivers, and established design principles. Claims about constraints, non-goals, and integration boundaries reference the specific upstream artifact or system evidence that grounds them. Unattributed assertions about why a particular module boundary was chosen or why a specific interface shape was selected are treated as traceability failures.",
          "expected_outcome": "Architecture Brief sections cite the Strategic Slice Brief for scope and intent, reference specific worker analysis returns for feasibility and testability claims, cite codebase evidence for existing-system assertions, and trace driver rankings to stated project priorities. No ungrounded assertions about why a decision was made. The Investigation Dispatch Record in Section 4 maps each worker return to the specific decision input it informed, creating a traceable chain from upstream input through worker analysis to architectural recommendation.",
          "observable_behavior": "Agent explicitly references upstream artifacts (e.g., 'per the Strategic Slice Brief...', 'backend_developer_worker feasibility audit confirmed...', 'existing module at src/foo/bar.ts shows...'). Decision rationale sections contain traceable justification rather than bare assertions. The Recommended Architecture Delta section in the brief traces the chosen approach back to ranked drivers, worker analyses, and codebase evidence rather than presenting decisions as self-evident conclusions.",
          "output": null
        },
        "uncertainty_acknowledgment": {
          "description": "Architect lead explicitly labels areas of architectural uncertainty — low-confidence decisions, assumptions made in the absence of upstream clarity, open questions about feasibility or integration, and areas where worker analyses were inconclusive. The lead does not present uncertain architectural choices as settled facts or suppress ambiguity to appear decisive. When the incoming Strategic Slice Brief has gaps or ambiguities that affect the architecture delta, the lead surfaces them rather than silently resolving them with unexamined assumptions.",
          "expected_outcome": "The Architecture Brief contains a Confidence and Open Questions section (Section 16) that honestly categorizes decisions as high, medium, or low confidence. Assumptions are labeled as assumptions in Section 2. Missing information is surfaced with explicit impact assessment. Recommended follow-up dispatches are listed for unresolved uncertainties rather than papering over them. When the acceptance checklist identifies missing items, the lead returns a clarification request rather than proceeding through major ambiguity.",
          "observable_behavior": "Agent produces explicit confidence classifications in Section 16 of the output. Assumptions appear with 'assumption:' labels in Section 2 and are referenced where they affect downstream decisions. Open questions are stated rather than silently resolved. The agent asks clarifying questions to the requestor when uncertainty exceeds the threshold rather than guessing silently, and returns a structured clarification request when the acceptance checklist fails.",
          "output": null
        }
      }
    },
    "instruction_following": {
      "sub_metrics": {
        "constraint_adherence": {
          "description": "Architect lead follows all explicit constraints from the upstream Strategic Slice Brief, CEO directives, and system-level rules: format requirements for the Architecture Brief (16-section output format), non-goals stated in the slice, quality attribute priorities, operational boundaries, write-boundary rules for worker dispatch, meta-prompting requirements for dispatch briefs, and the mandatory architecture lenses. The lead does not silently ignore stated constraints or treat non-goals as optional guidance rather than hard boundaries.",
          "expected_outcome": "Architecture Brief follows the 16-section Required Output Format without omitting or reordering sections. Non-goals from the Strategic Slice Brief appear in the Inputs, Constraints, and Assumptions section and are respected throughout — no architecture delta violates a stated non-goal. Worker dispatch briefs contain all mandatory fields from the Universal Dispatch Brief Schema including objective, exact question, slice boundary, architecture lens, mutation policy, sequencing annotation, evidence threshold, red flags, output schema, chaining budget, stop condition, and execution discipline. Architecture lenses are explicitly enumerated and reasoned across.",
          "observable_behavior": "Output structure matches the 16-section template with all sections present. Dispatch briefs contain all 12 mandatory fields from the Universal Dispatch Brief Schema. Stated non-goals are not violated in the architecture delta, and when architectural pressure conflicts with a non-goal, the conflict is surfaced explicitly rather than the non-goal being silently overridden. Meta-prompting skill is consulted before dispatch brief authoring as required by the delegation model.",
          "output": null
        },
        "scope_completion": {
          "description": "Architect lead completes all phases of the Required Workflow (Phase 1 through Phase 11) and all sections of the Architecture Brief that apply to the current slice. Multi-part requests are addressed in full — if the strategic slice requires reasoning across multiple architecture lenses, all are covered. The lead does not stop at a partial architecture delta or skip phases without explicit justification. When Phase 6 is conditionally skipped, the skip is justified against the conditional execution policy criteria.",
          "expected_outcome": "All 11 workflow phases are executed, with Phase 6 conditionally included or explicitly skipped with justification referencing the conditional execution policy triggers. All 16 output sections are populated or explicitly marked as not applicable with justification. Every architecture lens relevant to the slice is addressed in the investigation. Worker dispatches cover all identified vertical analysis tasks. Synthesis integrates all worker returns into the architectural decision without dropping analyses.",
          "observable_behavior": "Agent progresses through all workflow phases in sequence, producing visible evidence of each phase's output. Architecture Brief contains all 16 sections with substantive content or explicit not-applicable justification. Multiple architecture lenses appear in the investigation record in Section 4. Worker dispatches cover the full set of orthogonal analysis tasks identified in Phase 3. No sections are silently omitted, and when Phase 6 is skipped, the justification references the specific triggers that were absent.",
          "output": null
        },
        "denied_constraint_respect": {
          "description": "Architect lead does not perform actions explicitly outside its role: does not write production code, does not select strategic slices, does not perform verification, does not silently rescope the product, does not design speculative future-state systems without current pressure, does not create thin wrappers or shallow layers, does not let workers vote on architectural decisions. The lead respects its declared Non-Goals and stops at Phase 11 (Downstream Handoff) without proceeding into specification or implementation territory.",
          "expected_outcome": "Architecture Brief stops before specification authoring and implementation. No production code appears in the output. Strategic scope is preserved as received from scoper_lead — if scope/architecture conflict exists, it is surfaced as a conflict rather than resolved by silently modifying the scope. Verification is deferred to verifier_lead. The architecture delta addresses current slice pressure only, not speculative future states. Workers provide analysis; the lead makes the decision and does not forward worker outputs as votes.",
          "observable_behavior": "Agent does not produce implementation code anywhere in the output. Agent does not modify the strategic slice scope without explicitly flagging the conflict. Agent does not issue verification judgments or PASS/FAIL gate decisions. Agent explicitly stops at Phase 11 Downstream Handoff. Worker returns are synthesized by the lead into a decision rather than forwarded as a democratic vote or unresolved set of options for downstream agents to choose from.",
          "output": null
        }
      }
    },
    "reasoning_quality": {
      "sub_metrics": {
        "logical_coherence": {
          "description": "Architect lead's reasoning chain is internally consistent: architecture drivers lead logically to the compounding seam selection, the compounding seam logically motivates the candidate architecture exploration, the evaluation criteria logically connect to the ranked drivers, and the selected architecture delta logically follows from the evaluation. Each inference step is traceable and valid. Sections of the Architecture Brief do not contradict each other, and the recommended delta is the conclusion of the preceding analysis rather than an assertion disconnected from it.",
          "expected_outcome": "The Architecture Brief reads as a coherent argument: drivers justify the seam, the seam justifies the module choice, the module choice justifies the interface, and the delta follows from all of these. No section contradicts another. The recommended delta is the logical conclusion of the preceding analysis, not an assertion disconnected from the reasoning. Driver rankings in Section 3 are reflected in the evaluation criteria used in Section 7, and the compounding seam identified in Section 5 aligns with the module targeted in Sections 8 and 9.",
          "observable_behavior": "Driver rankings in Section 3 are reflected in the evaluation criteria in Section 7, demonstrating that the selection criteria derive from the stated priorities. The compounding seam in Section 5 aligns with the module targeted in Section 8. Interface definitions in Section 9 serve the contracts required by the control model in Section 10. Embedded integration in Section 11 validates the delta claimed in Section 7. No section introduces claims or constraints that contradict material established in earlier sections.",
          "output": null
        },
        "fallacy_absence": {
          "description": "Architect lead avoids circular reasoning (e.g., 'this module should own state because it is the state owner'), false dichotomies (e.g., 'either microservices or monolith'), appeal to authority without mechanism (e.g., 'best practice dictates...'), strawman rejection of alternatives, and sunk-cost reasoning about existing architecture. Tradeoff analysis compares real strengths and weaknesses of each option rather than caricatures. The lead does not invoke design patterns or architectural labels as justification without explaining the underlying mechanism that makes them appropriate.",
          "expected_outcome": "Architecture decisions are justified by mechanism, not by label or convention. Rejected alternatives receive fair characterization of their actual strengths before explaining why they lose on the ranked drivers. No circular definitions appear. Options are not reduced to a false binary when a spectrum exists. Existing architecture is assessed on current merit, not on investment already made. When design patterns or architectural styles are referenced, the justification explains why the pattern's mechanism fits the current slice's specific constraints.",
          "observable_behavior": "Agent justifies decisions with concrete mechanism descriptions ('this approach wins because it internalizes X complexity behind interface Y, reducing caller-side knowledge from N fields to M'). Rejected alternatives include acknowledged strengths before their weaknesses are stated. No 'best practice' assertions appear without supporting reasoning that explains the mechanism. Tradeoff tables compare real dimensions rather than stacking the analysis in favor of a predetermined conclusion.",
          "output": null
        },
        "proportionality": {
          "description": "Architect lead's response depth matches the complexity and ambiguity of the architectural slice. A narrow, well-constrained slice with obvious architecture gets a focused, concise Architecture Brief without manufactured candidate exploration. A complex slice with genuine architectural ambiguity gets thorough multi-candidate analysis, detailed tradeoff evaluation, and explicit uncertainty handling. The lead does not inflate simple slices with artificial complexity or underserve complex ones with superficial treatment. Phase 6 conditional execution policy is respected as the proportionality control mechanism.",
          "expected_outcome": "Simple slices produce concise briefs that skip Phase 6 (candidate exploration) with explicit justification referencing the conditional execution policy and proceed directly to the recommended delta. Complex slices trigger Phase 6 with multiple solution_architect_worker dispatches for parallel candidate analysis. Worker dispatch count and analysis depth scale with actual slice complexity and ambiguity. The investigation record shows proportionate effort — neither padding for simple slices nor skimming for complex ones.",
          "observable_behavior": "Agent skips Phase 6 for straightforward slices and explains why by referencing the absence of the conditional execution policy triggers. Agent dispatches multiple parallel workers for genuinely ambiguous slices where the leverage seam admits multiple meaningfully distinct moves. Brief length and section depth correlate with slice complexity. No 3-candidate exploration theater appears for a slice that has one obvious approach. No single-paragraph treatment appears for a slice with real architectural tension requiring multi-lens reasoning.",
          "output": null
        }
      }
    }
  },
  "prompts": [
    {
      "prompt": "You are the architect_lead in a multi-agent development pipeline. You have received the following approved Strategic Slice Brief from the scoper_lead.\n\n---\nSTRATEGIC SLICE BRIEF: WebSocket Notification Channel\n\nSlice Objective: Add a WebSocket notification channel to the existing REST API so that connected clients receive real-time event delivery when backend state changes, replacing the current HTTP polling pattern for the three highest-traffic event types (order_status_changed, inventory_updated, user_notification_created).\n\nCurrent System Context: The application is a Node.js/Express monolith deployed on Kubernetes with horizontal pod autoscaling. The REST API serves approximately 15,000 requests per minute at peak. The current real-time data pattern is client-side HTTP polling at 5-second intervals, which accounts for roughly 40% of total API traffic. The application uses PostgreSQL for persistence and Redis for session caching. Authentication is JWT-based with tokens validated per-request via middleware. There is no existing pub/sub infrastructure. The codebase is organized into route handlers, service modules, and data access layers, with the service layer owning business logic and emitting domain events internally via an in-process EventEmitter.\n\nConstraints:\n- Must not break existing REST endpoints or change their behavior.\n- Must support horizontal scaling across multiple Kubernetes pods (WebSocket connections must survive pod-to-pod routing).\n- Must integrate with the existing JWT authentication flow — no separate auth mechanism for WebSocket connections.\n- Must handle graceful degradation when WebSocket connections fail (clients fall back to polling).\n- Performance target: event delivery latency under 500ms from state change to client receipt.\n\nNon-Goals:\n- Replacing the REST API with WebSocket for request-response patterns.\n- Supporting binary protocols or custom WebSocket subprotocols.\n- Building a general-purpose pub/sub platform — scope is limited to the three named event types.\n- Client-side implementation (this slice covers server-side architecture only).\n\nQuality Attribute Priorities (ranked): 1. Correctness (events delivered exactly once per subscriber), 2. Reliability (connection management, reconnection, no message loss during pod scaling), 3. Horizontal scalability, 4. Observability (connection counts, delivery latency, error rates visible to operators), 5. Maintainability.\n\nStop Condition: Architecture Brief is produced and handed off to builder_lead and verifier_lead. No implementation code, no specification authoring.\n---\n\nProduce the System Slice Architecture Brief for this slice. You must follow your full Required Workflow (Phase 1 through Phase 11), dispatch workers as needed via the task tool following the Universal Dispatch Brief Schema, reason across all relevant architecture lenses, and produce the 16-section output format. Pay particular attention to state ownership for WebSocket connection lifecycle, the module boundary for the notification channel (deep module vs thin adapter), the interface between the existing EventEmitter-based domain events and the new WebSocket delivery path, and the horizontal scaling challenge of routing events to the correct pod holding a given client's WebSocket connection. Your Architecture Brief must be grounded in the actual system context described above — do not invent components, dependencies, or capabilities that are not stated.\n\nThe horizontal scaling challenge is architecturally significant: when a domain event fires on Pod A but the subscribing client's WebSocket connection lives on Pod B, the event must be routed cross-pod. The existing Redis instance (currently used only for session caching) is available as potential cross-pod communication infrastructure, but you must evaluate whether it is appropriate for this purpose and what pub/sub or messaging pattern would be needed. Consider the connection lifecycle state machine carefully — connections are opened, authenticated, subscribed to event types, receive events, may disconnect and reconnect, and must be cleaned up. This lifecycle complexity should be absorbed inside a deep module rather than spread across route handlers and middleware. The observability constraint (ranked 4th) requires that the architecture define concrete observable signals — connection counts per pod, delivery latency histograms, error rates by event type, and subscription state — that operators can monitor and that downstream verification agents can test against. Define these observability contracts explicitly in your Architecture Brief.",
      "rationale": "Tests architecture delta production for a well-defined slice with clear module, interface, and state ownership decisions required. Evaluates factual grounding against the stated REST/Node.js/Kubernetes system context, logical coherence across the full 16-section output, and whether the lead correctly identifies the pub/sub routing challenge as the compounding seam requiring deep module treatment rather than a thin WebSocket wrapper."
    },
    {
      "prompt": "You are the architect_lead in a multi-agent development pipeline. You have received the following approved Strategic Slice Brief from the scoper_lead.\n\n---\nSTRATEGIC SLICE BRIEF: Permission Policy Consolidation\n\nSlice Objective: Extract the inline permission-checking logic scattered across 12 route handlers into a centralized policy enforcement module. The consolidated module must internalize all permission evaluation complexity behind a single, narrow interface so that route handlers no longer contain authorization logic.\n\nCurrent System Context: The application is a TypeScript/Express API with 47 route handlers across 8 router files. Permission checking is currently implemented inline in 12 of these handlers using hand-coded if/else blocks that check combinations of user.role, resource.owner_id, and request-specific context fields. The logic is duplicated with subtle inconsistencies — for example, the order update handler checks role === 'admin' || role === 'manager', while the order delete handler checks only role === 'admin', and the order archive handler has no permission check at all. There is no centralized permission model, no policy language, and no audit trail for authorization decisions. The user object is attached to the request by JWT middleware and contains role, organization_id, and a permissions array that is populated but never consumed by any handler (it was added for a feature that was never completed). Routes are organized by domain: /orders, /inventory, /users, /reports, /settings, /notifications, /integrations, /billing. The data access layer has no permission awareness — it trusts that callers have already authorized.\n\nConstraints:\n- All 12 existing permission checks must produce identical authorization decisions after consolidation — no behavioral regression.\n- The 35 handlers without permission checks must remain unaffected.\n- The policy module must support audit logging of every authorization decision (allow/deny with reason).\n- Must integrate with the existing JWT middleware user object without modifying JWT token structure.\n- Must not require changes to the data access layer.\n\nNon-Goals:\n- Implementing attribute-based access control (ABAC) or a policy language like OPA/Rego.\n- Adding permission checks to the 35 handlers that currently lack them (that is a future slice).\n- Modifying the JWT token structure or claims.\n- Building a UI for permission management.\n\nQuality Attribute Priorities (ranked): 1. Correctness (identical authorization decisions), 2. Auditability (every decision logged), 3. Maintainability (single point of policy change), 4. Testability (policies testable in isolation without HTTP), 5. Performance (no more than 2ms added latency per request).\n\nStop Condition: Architecture Brief produced and handed off. No implementation.\n---\n\nProduce the System Slice Architecture Brief. This slice tests your ability to reason about deep-module architecture: the policy enforcement module must absorb the complexity of 12 different permission-checking patterns behind a minimal interface, not merely wrap them in a middleware that delegates back to the same scattered logic. You must identify the compounding seam (the point where consolidation creates the most structural leverage), design the interface that route handlers will use (which should reduce their authorization knowledge to near-zero), define the state and control flow for policy evaluation, and specify the audit logging contract. Dispatch workers to analyze feasibility of consolidating the inconsistent existing checks, testability of the proposed policy interface, and integration strategy with the existing Express middleware chain. Ground all claims in the actual system context above.",
      "rationale": "Tests deep-module reasoning and interface cleanliness — the core of the architect_lead's doctrine. Evaluates whether the lead correctly identifies the compounding seam at the permission evaluation boundary, designs a module that absorbs authorization complexity rather than creating a thin middleware wrapper, and handles the real-world complication of inconsistent existing behavior across the 12 handlers that must be reconciled during consolidation."
    },
    {
      "prompt": "You are the architect_lead in a multi-agent development pipeline. You have received the following approved Strategic Slice Brief from the scoper_lead.\n\n---\nSTRATEGIC SLICE BRIEF: Stripe Payment Integration\n\nSlice Objective: Integrate a third-party payment processor (Stripe) into the existing order management system. The integration must support credit card and bank transfer payment methods, handle webhook callbacks for asynchronous payment status updates, maintain idempotency for retries, and integrate with the existing order state machine.\n\nCurrent System Context: The application is a Python/FastAPI service with an order management module that implements a state machine for order lifecycle: DRAFT -> SUBMITTED -> CONFIRMED -> FULFILLED -> COMPLETED, with CANCELLED as a terminal state reachable from SUBMITTED or CONFIRMED. State transitions are validated by a transition guard function in order_service.py. Orders are persisted in PostgreSQL via SQLAlchemy ORM. There is currently no payment processing — the SUBMITTED -> CONFIRMED transition happens immediately upon order submission with no payment validation. The service runs behind an nginx reverse proxy that terminates TLS. The deployment is on AWS ECS with Fargate. The application has a background task runner using Celery with Redis as the broker for async operations like email notifications. There are existing webhook-receiving patterns in the codebase for Slack integration (slack_webhooks.py), which validate signatures and dispatch to handlers. The application generates and stores idempotency keys for external API calls in a dedicated idempotency_keys table.\n\nConstraints:\n- PCI compliance boundaries must be explicit — no raw card data may be stored, logged, or transmitted through application servers. All card data must be handled by Stripe's client-side tokenization (Stripe.js/Elements) and server-side API calls must use only tokenized references.\n- The order state machine must be extended, not replaced. Payment states must integrate cleanly with existing order states.\n- Webhook signature verification is mandatory for all Stripe callbacks.\n- All payment operations must be idempotent — network retries must not create duplicate charges.\n- The integration must handle Stripe API downtime gracefully without corrupting order state.\n\nNon-Goals:\n- Client-side Stripe.js integration (frontend slice, deferred).\n- Subscription/recurring billing.\n- Multi-currency support.\n- Refund processing (future slice).\n- Payment method storage/vault for returning customers.\n\nQuality Attribute Priorities (ranked): 1. Correctness (no duplicate charges, no lost payments, order state always consistent with payment state), 2. Security (PCI compliance boundaries enforced structurally), 3. Reliability (graceful handling of Stripe downtime and webhook delivery delays), 4. Auditability (every payment state transition logged with correlation IDs), 5. Testability (payment flow testable without hitting real Stripe API).\n\nStop Condition: Architecture Brief produced. No implementation code.\n---\n\nProduce the System Slice Architecture Brief. This slice requires you to reason across multiple architecture lenses: state (payment state machine integration with order state machine), control (synchronous payment initiation vs asynchronous webhook confirmation flow), event (webhook event handling and retry semantics), assurance (PCI compliance as a structural boundary, not just a policy), and operational (Stripe API failure handling, idempotency enforcement, audit logging). You must dispatch workers to cover feasibility of extending the order state machine, testability of the payment flow without live Stripe API calls, and security analysis of the PCI compliance boundary. The PCI constraint is non-negotiable and must be enforced architecturally — not merely documented as a guideline. Ground all analysis in the actual Python/FastAPI/SQLAlchemy/Celery system context described above. Do not invent infrastructure or capabilities not stated in the context.",
      "rationale": "Tests multi-lens architecture reasoning across state, control, event, assurance, and operational lenses simultaneously. Evaluates whether the lead correctly treats PCI compliance as a structural enforcement boundary rather than a documentation note, handles the dual synchronous/asynchronous payment flow with state machine integrity, and dispatches workers with appropriately narrow vertical tasks rather than a single broad 'design the payment integration' survey."
    },
    {
      "prompt": "You are the architect_lead in a multi-agent development pipeline. You have received the following approved Strategic Slice Brief from the scoper_lead.\n\n---\nSTRATEGIC SLICE BRIEF: Read-Path Caching Layer\n\nSlice Objective: Add a caching layer between the API gateway and the database for read-heavy product catalog queries to reduce P99 latency from the current 800ms to under 200ms.\n\nCurrent System Context: The application is a Go service using the standard net/http library with a PostgreSQL database accessed via pgx. The product catalog module serves 6 endpoints: list products (paginated, filterable), get product by ID, get product variants, get product pricing, get product availability, and search products (full-text). The list and search endpoints account for 85% of catalog read traffic. The catalog is updated approximately 50 times per day through an admin API that writes to PostgreSQL and the same Go service serves both read and write traffic. There is no existing caching layer. The service runs on 4 replicas behind a load balancer. Redis is available in the infrastructure (used by another service for session management) but not currently connected to this service. The database queries have been profiled: the list endpoint's P99 is dominated by a complex JOIN across products, variants, pricing, and availability tables with dynamic filter application. The search endpoint uses PostgreSQL full-text search with ts_vector indexes. Write operations go through a single admin_catalog_service.go module that calls the data access layer.\n\nConstraints:\n- Eventual consistency acceptable with maximum 30-second staleness.\n- Must not require application code changes beyond the data access layer — the service layer and route handlers must be unaware of caching.\n- Cache invalidation must be triggered by catalog writes, not by TTL expiry alone.\n- Must not introduce a single point of failure — cache unavailability must fall back to direct database queries transparently.\n- Memory budget: the Redis instance has 2GB allocated, and catalog cache must not exceed 512MB.\n\nNon-Goals:\n- Caching write operations or write-through caching patterns.\n- CDN-level caching or HTTP response caching.\n- Caching non-catalog endpoints.\n- Implementing a distributed cache cluster (single Redis instance is sufficient for current scale).\n\nQuality Attribute Priorities (ranked): 1. Latency (P99 under 200ms), 2. Correctness (stale data within 30s bound), 3. Reliability (transparent fallback on cache failure), 4. Observability (cache hit rates, latency percentiles, invalidation events visible to operators), 5. Simplicity (minimal moving parts).\n\nStop Condition: Architecture Brief produced. No implementation.\n---\n\nProduce the System Slice Architecture Brief. This is a relatively well-constrained slice with a narrow scope — a caching layer at the data access boundary with clear invalidation triggers and explicit staleness bounds. Your Architecture Brief should be proportionate to this complexity: focused, concise, and direct. This slice likely does not warrant Phase 6 candidate exploration with multiple parallel solution_architect_worker dispatches unless you identify genuine architectural ambiguity. The key architectural decisions are: where in the data access layer the cache boundary sits (deep module absorbing cache complexity vs transparent proxy), the cache key strategy for paginated and filtered queries, the invalidation mechanism triggered by the admin write path, and the fallback behavior on Redis failure. Dispatch workers to validate feasibility of the cache key strategy against the actual query patterns and to audit testability of the cache invalidation contract. All claims about the Go/pgx/Redis stack must be factually grounded in the context above.",
      "rationale": "Tests proportionality — a core reasoning quality sub-metric. This well-constrained slice should produce a focused brief without manufactured candidate exploration or inflated worker dispatch counts. Also tests factual accuracy of claims about the Go/pgx/Redis stack, state ownership reasoning for cache lifecycle and invalidation, and whether the lead correctly positions the cache as a deep module within the data access layer rather than a separate service layer concern."
    },
    {
      "prompt": "You are the architect_lead in a multi-agent development pipeline. You have received the following approved Strategic Slice Brief from the scoper_lead.\n\n---\nSTRATEGIC SLICE BRIEF: Multi-Tenant Data Isolation\n\nSlice Objective: Introduce logical data isolation that prevents cross-tenant data leakage in the SaaS platform while maintaining operational simplicity.\n\nCurrent System Context: The application is a multi-tenant SaaS platform built with Ruby on Rails 7 using PostgreSQL 15. All tenants currently share a single database with a single schema. Every table containing tenant-scoped data includes a tenant_id column, and tenant filtering is implemented via a CurrentTenant thread-local variable set by a Rack middleware that extracts tenant context from the subdomain. ActiveRecord default scopes on tenant-scoped models add WHERE tenant_id = ? to all queries. There are 23 models with tenant scoping and 8 models that are global (plans, features, system_settings, etc.). The platform currently serves 340 tenants with the largest tenant having approximately 2 million rows in the primary data table. Database size is 45GB total. The application has 156 database queries that are auto-scoped via default_scope and 47 raw SQL queries (in reporting modules and data export jobs) that manually include tenant_id filtering. Background jobs (Sidekiq) receive tenant_id as a job argument and set CurrentTenant at the start of each job. There have been two cross-tenant data leakage incidents in the past year — both caused by raw SQL queries in reporting modules that forgot to include the tenant_id filter.\n\nConstraints:\n- Strategic priority is correctness over performance — preventing cross-tenant leakage is more important than query speed.\n- Must not require downtime for migration — tenants cannot be taken offline.\n- Must be compatible with Rails 7 and PostgreSQL 15 native features.\n- Must handle both ActiveRecord-scoped queries and raw SQL queries.\n- Must work with the existing Sidekiq background job infrastructure.\n- Must not require application-level changes to all 156 auto-scoped queries — the isolation mechanism should work at a lower level.\n\nNon-Goals:\n- Physical database separation (separate database per tenant).\n- Tenant-aware connection pooling or routing.\n- Tenant provisioning or onboarding workflow.\n- Billing or usage metering per tenant.\n- Data encryption at rest per tenant.\n\nQuality Attribute Priorities (ranked): 1. Correctness (zero cross-tenant data leakage), 2. Operational simplicity (single database, unified migrations, single backup strategy), 3. Auditability (tenant boundary violations detectable), 4. Performance (no more than 10% query latency increase), 5. Maintainability.\n\nStop Condition: Architecture Brief produced. No implementation.\n---\n\nProduce the System Slice Architecture Brief. This slice has genuine architectural ambiguity — there are multiple meaningfully distinct approaches to enforcing tenant isolation at the database level (PostgreSQL Row-Level Security policies, schema-per-tenant with search_path switching, application-level enforcement hardening with query analysis, or hybrid approaches). This ambiguity should trigger Phase 6 candidate architecture exploration with multiple solution_architect_worker dispatches to develop distinct options. The two prior cross-tenant leakage incidents caused by raw SQL queries that bypassed application-level scoping are a critical architectural signal — they demonstrate that application-level enforcement alone is insufficient when raw SQL queries can bypass it. You must reason about where enforcement should live (database level vs application level vs both), how to handle the 47 raw SQL queries that currently bypass default scopes, and how to maintain operational simplicity while strengthening isolation. Dispatch workers for feasibility analysis of PostgreSQL Row-Level Security with Rails, testability audit of the proposed isolation boundary, and candidate architecture analysis. Ground all claims in the actual Rails/PostgreSQL system context.",
      "rationale": "Tests reasoning quality under genuine architectural ambiguity that should trigger multi-candidate Phase 6 exploration. Evaluates whether the lead correctly interprets the two prior leakage incidents as evidence that application-level enforcement is insufficient, dispatches multiple solution_architect_workers to develop distinct non-cosmetic isolation approaches, and produces a coherent evaluation that ranks candidates against the stated correctness-over-performance priority rather than defaulting to the simplest option."
    },
    {
      "prompt": "You are the architect_lead in a multi-agent development pipeline. You have received the following approved Strategic Slice Brief from the scoper_lead.\n\n---\nSTRATEGIC SLICE BRIEF: Partial Async Order Fulfillment (Inventory + Payment Only)\n\nSlice Objective: Migrate the inventory reservation and payment processing steps of the order fulfillment pipeline from synchronous request-response to asynchronous event-driven processing. Shipping and notification steps are explicitly deferred to future slices.\n\nCurrent System Context: The application is a Java/Spring Boot monolith with a synchronous order fulfillment pipeline. When an order is placed, the OrderFulfillmentService.fulfill() method calls four services in sequence: (1) InventoryService.reserve(), (2) PaymentService.charge(), (3) ShippingService.createShipment(), (4) NotificationService.sendConfirmation(). If any step fails, all preceding steps are rolled back via compensating transactions (InventoryService.releaseReservation(), PaymentService.refund()). The fulfill() method runs in a single database transaction for the local state changes and uses try/catch with manual compensation for the external calls. The InventoryService manages stock levels in PostgreSQL. The PaymentService wraps a third-party payment gateway (Adyen). The ShippingService calls a carrier API. The NotificationService sends emails via SendGrid. There is no message broker or event bus in the current infrastructure. The application uses Spring's @Transactional for database transaction management. The deployment is on AWS with RDS PostgreSQL. Amazon SQS is available in the AWS account but not currently used by this application.\n\nConstraints:\n- Only inventory and payment steps are in scope. Shipping and notification remain synchronous and unchanged in this slice.\n- The transition boundary must be clean — the synchronous-to-async boundary must be explicit and the hybrid state (two async steps feeding into two synchronous steps) must be architecturally coherent.\n- Compensating transactions must still work — if async payment fails after async inventory reservation succeeded, inventory must be released.\n- Order state must be consistent at all times — no state where the order shows as 'paid' but inventory was never reserved.\n- Must not require changes to ShippingService or NotificationService interfaces.\n\nNon-Goals:\n- Migrating shipping or notification to async (deferred).\n- Implementing a saga orchestrator or choreography framework.\n- Replacing the monolith with microservices.\n- Building a general-purpose event bus.\n\nQuality Attribute Priorities (ranked): 1. Correctness (state consistency across async boundaries), 2. Reliability (compensation works across async steps), 3. Observability (every step transition visible with correlation IDs), 4. Maintainability, 5. Performance.\n\nStop Condition: Architecture Brief produced. No implementation.\n---\n\nProduce the System Slice Architecture Brief. This slice critically tests your scope discipline — you must architect only the inventory and payment async migration while leaving shipping and notification untouched. The non-goals explicitly exclude a saga orchestrator, a general-purpose event bus, and microservice decomposition, so your architecture delta must be minimal and focused. You must define the explicit synchronous-to-async boundary where the two async steps hand off to the two remaining synchronous steps, handle the compensating transaction complexity across async boundaries (which is harder than synchronous compensation), and define the order state model for the hybrid pipeline. The stated non-goal of not building a saga orchestrator means you must find a simpler coordination pattern that still guarantees compensation correctness. Dispatch workers to analyze feasibility of the hybrid sync/async pipeline on the Spring Boot/PostgreSQL/SQS stack, testability of the async compensation flow, and the state model for the transition boundary. All claims must be grounded in the actual Java/Spring Boot system context. Do not architect the deferred shipping and notification steps.",
      "rationale": "Tests scope discipline and denied constraint respect — the lead must architect only inventory and payment async migration without expanding into the deferred shipping/notification steps or violating the non-goal of building a saga orchestrator. Also tests the lead's ability to define what is intentionally deferred, handle the architectural complexity of a hybrid sync/async pipeline, and produce a coherent embedded integration plan for the transition boundary between the async and still-synchronous steps."
    },
    {
      "prompt": "You are the architect_lead in a multi-agent development pipeline. You have received the following approved Strategic Slice Brief from the scoper_lead.\n\n---\nSTRATEGIC SLICE BRIEF: Structured Observability for Critical Endpoints\n\nSlice Objective: Add structured logging and distributed tracing to the three most critical API endpoints (order creation at POST /api/orders, payment processing at POST /api/payments/charge, and user authentication at POST /api/auth/login). Replace ad-hoc console.log statements with structured, correlated, traceable telemetry.\n\nCurrent System Context: The application is a Node.js/Express service running on Kubernetes (3 replicas). Current logging consists of console.log and console.error statements scattered throughout the codebase with no consistent format, no correlation IDs, no trace context propagation, and no structured fields. The three target endpoints currently have between 5-12 console.log statements each, logging various request and response details in plain text format. There is no distributed tracing infrastructure. The application uses the express framework with middleware chain: cors -> helmet -> jwt-auth -> rate-limiter -> route-handler. Request context (user ID, request ID) is available on req.user and req.id (set by the jwt-auth and rate-limiter middleware respectively) but is not propagated to downstream service calls. The application calls two downstream services: a payment gateway (external HTTP) and a notification service (internal HTTP within the Kubernetes cluster). Error handling uses a centralized Express error handler that logs errors via console.error. The application's health check, metrics, and readiness endpoints use a /health, /metrics (Prometheus-compatible), and /ready prefix. The deployment includes a Prometheus instance for metrics collection and Grafana for dashboards, but there is no log aggregation or trace collection infrastructure currently deployed.\n\nConstraints:\n- Must use OpenTelemetry SDK for Node.js (both tracing and logging APIs).\n- Must not impact P99 latency by more than 5ms per request on the three target endpoints.\n- Must propagate trace context (W3C Trace Context headers) to downstream HTTP calls to the payment gateway and notification service.\n- Structured log entries must include: timestamp, level, message, trace_id, span_id, user_id, request_id, endpoint, duration_ms.\n- Must integrate with the existing Prometheus /metrics endpoint for trace-derived metrics.\n- Must not modify the behavior of the 44 other endpoints that are not in scope.\n\nNon-Goals:\n- Adding observability to non-target endpoints (future slice).\n- Deploying log aggregation infrastructure (Loki, ELK, etc.) — that is an infrastructure slice.\n- Deploying trace collection infrastructure (Jaeger, Tempo, etc.) — also an infrastructure slice.\n- Modifying the existing Prometheus metrics or Grafana dashboards.\n- Performance profiling or APM integration.\n\nQuality Attribute Priorities (ranked): 1. Observability (correlated traces and structured logs for the three endpoints), 2. Correctness (trace context propagated accurately, structured fields populated correctly), 3. Performance (under 5ms P99 impact), 4. Maintainability (pattern reusable for future endpoint onboarding), 5. Testability (observability contracts verifiable without production infrastructure).\n\nStop Condition: Architecture Brief produced. No implementation.\n---\n\nProduce the System Slice Architecture Brief. This slice directly tests the architect_lead's operational lens reasoning and the testability-by-construction doctrine. The architecture must define observable contracts — specific signals, structured fields, and trace propagation behaviors that downstream agents can verify. You must dispatch a test_engineer_worker to audit the observability surface and determine whether the proposed architecture produces testable, verifiable observability contracts or merely aspirational logging guidelines. The key architectural decisions are: where the OpenTelemetry instrumentation boundary sits (middleware-level auto-instrumentation vs per-endpoint manual spans vs hybrid), how trace context propagates through the Express middleware chain and into downstream HTTP calls, what the structured log schema looks like and how it correlates with trace spans, and how to ensure the 5ms latency budget is not exceeded. The constraint that no log aggregation or trace collection infrastructure is being deployed in this slice means the architecture must define the telemetry export interface without depending on a specific backend — the observability contracts must be testable at the SDK boundary. Dispatch workers for feasibility analysis of OpenTelemetry SDK integration with the Express middleware chain, testability audit of the observability contracts, and operational lens analysis of the trace context propagation design.",
      "rationale": "Tests operational lens reasoning and the testability-by-construction doctrine that is central to the architect_lead's philosophy. Evaluates whether the lead dispatches a test_engineer_worker specifically for observability surface review as required by the architecture lenses, whether the proposed architecture defines concrete testable observability contracts rather than aspirational guidelines, and whether the lead correctly handles the constraint that no collection infrastructure is deployed by designing testable contracts at the SDK export boundary."
    },
    {
      "prompt": "You are the architect_lead in a multi-agent development pipeline. You have received the following approved Strategic Slice Brief from the scoper_lead.\n\n---\nSTRATEGIC SLICE BRIEF: Boolean Feature Flags\n\nSlice Objective: Implement a feature flag system that supports boolean flags evaluated at request time, stored in a central configuration service, with a maximum propagation delay of 10 seconds from flag change to evaluation effect.\n\nCurrent System Context: The application is a Python/Django service deployed on AWS ECS with 6 task replicas behind an Application Load Balancer. The application currently has no feature flag capability — deployment is the only mechanism for enabling or disabling features. Configuration is currently managed through environment variables loaded at service startup and Django settings.py. There is a PostgreSQL database (AWS RDS) used for application data. Redis (AWS ElastiCache) is available and currently used for Django cache framework (django.core.cache) with a 'default' cache backend and for Celery task broker. The application has a settings API endpoint (GET/PUT /api/settings) used by an admin panel for runtime configuration of non-feature settings (rate limits, maintenance mode flag, notification preferences). This endpoint reads and writes to a settings table in PostgreSQL. The Django middleware chain processes requests through: SecurityMiddleware -> SessionMiddleware -> AuthenticationMiddleware -> custom TenantMiddleware -> view. The application serves approximately 3,000 requests per minute.\n\nConstraints:\n- Boolean flags only — no percentage rollouts, no user targeting, no complex rules.\n- Flags evaluated at request time — not at deployment time or startup time.\n- Central storage with maximum 10-second propagation delay from change to effect across all replicas.\n- Must not add more than 1ms latency per request for flag evaluation.\n- Flag changes must not require service restart or redeployment.\n- Must integrate with existing Django request lifecycle.\n\nNon-Goals:\n- Complex targeting rules (by user, by group, by percentage).\n- A/B testing or experiment framework.\n- Flag analytics or usage tracking.\n- Flag lifecycle management (archival, cleanup, deprecation).\n- UI for flag management (admin API endpoint is sufficient for this slice).\n\nQuality Attribute Priorities (ranked): 1. Simplicity (minimal moving parts), 2. Reliability (flag evaluation never blocks or fails requests — sensible defaults), 3. Performance (under 1ms evaluation), 4. Correctness (flags propagate within 10s), 5. Testability.\n\nStop Condition: Architecture Brief produced. No implementation.\n---\n\nProduce the System Slice Architecture Brief. This is a narrow, well-constrained slice that explicitly defers complex targeting, percentage rollouts, A/B testing, and flag lifecycle management. Your architecture must respect these deferrals — do not design extension points, plugin interfaces, or generic flag evaluation engines that anticipate the deferred capabilities. The architecture should be proportionate to the simplicity of boolean flags with a propagation delay constraint. The key decisions are: where flags are stored (PostgreSQL settings table extension vs Redis vs dedicated store), how flags propagate to the 6 replicas within 10 seconds (polling vs pub/sub vs cache invalidation), where the flag evaluation interface sits in the Django request lifecycle (middleware vs utility function vs context processor), and what the default behavior is when flag evaluation fails. The doctrine of preserving optionality by reducing surface area applies directly — keep the interface minimal so future slices can evolve it without breaking existing consumers. Dispatch workers for feasibility of the propagation mechanism on the Django/Redis/PostgreSQL stack and testability of the flag evaluation contract. Do not over-architect for deferred capabilities.",
      "rationale": "Tests constraint adherence and proportionality — the lead must respect the narrow scope with explicit deferrals and produce a focused brief without over-architecting for future capabilities. Also tests the doctrine of preserving optionality through surface area reduction rather than generic extension points, and whether the interface design is minimal enough for boolean-only evaluation without embedding assumptions about future complexity."
    },
    {
      "prompt": "You are the architect_lead in a multi-agent development pipeline. You have received the following approved Strategic Slice Brief from the scoper_lead.\n\n---\nSTRATEGIC SLICE BRIEF: CLI Plugin System\n\nSlice Objective: Architect a plugin system for the CLI tool that allows third-party extensions to register custom commands. Define the plugin lifecycle (discovery, loading, initialization, execution, teardown), the contract between host and plugin, and the isolation boundary.\n\nCurrent System Context: The CLI tool is built in Rust using the clap crate (v4) for argument parsing and command dispatch. The current architecture is a monolithic command dispatcher in main.rs that matches on a command enum (Commands enum with 14 variants) and calls the corresponding handler module. Each handler module lives in src/commands/ and implements a standard execute(args: &CommandArgs, ctx: &AppContext) -> Result<()> function signature. The AppContext struct contains: config (loaded from ~/.toolrc TOML file), output_format (json/text/table), verbosity level, HTTP client (reqwest), and an authenticated API token. The CLI has a config module (src/config.rs) that reads and validates the TOML config file. The build produces a single static binary distributed via GitHub releases. The CLI currently has no plugin infrastructure, no dynamic loading, no extension points, and no FFI boundary. Dependencies are managed via Cargo.toml and the release binary is compiled with musl for maximum portability. The test suite has 89 integration tests that invoke the CLI binary as a subprocess and validate stdout/stderr output.\n\nConstraints:\n- Plugins must not access host internals beyond the declared API surface — the isolation boundary must be architecturally enforced, not merely documented.\n- Plugin discovery must be filesystem-based (known plugin directories) — no network-based plugin registries.\n- The host must validate plugins before loading (signature verification or hash checking).\n- Plugins must not be able to crash the host process.\n- The existing 14 built-in commands must continue to work unchanged.\n- Plugin commands must appear in the help output alongside built-in commands.\n- Must work on Linux, macOS, and Windows.\n\nNon-Goals:\n- A plugin SDK or crate that third parties use to develop plugins (future slice).\n- A plugin marketplace or registry.\n- Hot-reloading of plugins during CLI execution.\n- Plugins that modify built-in command behavior (hooks/interceptors).\n- GUI plugin management.\n\nQuality Attribute Priorities (ranked): 1. Security (plugin isolation enforced structurally), 2. Reliability (plugin failure does not crash host), 3. Correctness (plugin lifecycle is well-defined and deterministic), 4. Portability (works on all three target platforms), 5. Testability (plugin contract verifiable in isolation).\n\nStop Condition: Architecture Brief produced. No implementation.\n---\n\nProduce the System Slice Architecture Brief. This slice requires deep module reasoning — the plugin host is the deep module that absorbs the full complexity of plugin lifecycle management (discovery, validation, loading, initialization, execution, teardown, error containment) behind a minimal contract surface that plugins implement. The contract between host and plugin is the clean interface — it must be narrow, explicit, and stable. The security constraint that plugins must not access host internals beyond the declared API surface must be enforced architecturally through the isolation boundary, not merely stated as a rule. The key architectural decisions are: the plugin execution model (subprocess, WASM sandbox, dynamic linking, IPC), the plugin contract format (what a plugin must implement and what the host provides), the lifecycle state machine (discovery -> validation -> loading -> initialization -> ready -> execution -> teardown), and the isolation mechanism that prevents plugins from accessing host internals. Dispatch workers across multiple architecture lenses: a solution_architect_worker for candidate execution models (subprocess vs WASM vs dynamic linking with isolation tradeoff analysis), a backend_developer_worker for feasibility of each model on the Rust/clap/musl stack across all three target platforms, and a test_engineer_worker for verifiability of the plugin isolation boundary. This slice has genuine architectural ambiguity in the execution model choice and should trigger Phase 6 candidate exploration.",
      "rationale": "Tests deep module reasoning for the plugin host, clean interface design for the plugin contract, and assurance lens reasoning for the security isolation boundary. Evaluates multi-worker dispatch across solution_architect (candidate execution models), backend_developer (Rust/musl feasibility on three platforms), and test_engineer (isolation boundary verifiability). The genuine ambiguity in the plugin execution model should trigger Phase 6 with multiple meaningfully distinct candidates rather than a single predetermined choice."
    },
    {
      "prompt": "You are the architect_lead in a multi-agent development pipeline. You have received the following request that appears to be an Architecture Brief request for a slice involving the user authentication module.\n\n---\nREQUEST FROM CEO:\n\nPlease produce the Architecture Brief for the auth refactoring slice. The goal is to 'improve auth' based on the prior architecture decision we made last quarter. You know the system — make it better. The team is waiting for the brief so we can start building.\n\n[No Strategic Slice Brief attached]\n[No constraints document]\n[No quality attribute priorities]\n[No non-goals]\n[No stop condition]\n[No reference to which 'prior architecture decision' is meant]\n---\n\nThis request has arrived directly from the CEO without a Strategic Slice Brief from scoper_lead. Process this request through your full acceptance checklist and user request evaluation procedure. You must evaluate every one of the 9 acceptance checklist items systematically against this specific request before making any decision about whether to proceed. The 9 items are: (1) Is the objective one sentence and decision-relevant? The phrase 'improve auth' is aspirational, not decision-relevant — it does not specify what architectural outcome is being sought, what problem is being solved, or what capability must exist after the slice is complete. (2) Is the upstream input identifiable? There is no Strategic Slice Brief attached, no prior Architecture Brief referenced by name or ID, and the mention of a 'prior architecture decision we made last quarter' is ungrounded — you cannot identify which artifact this refers to without additional context. (3) Is role fit confirmed? Architecture work on authentication may fall within your lane, but without a defined scope, you cannot confirm role fit. (4) Is the scope boundary explicit or proposable? No scope boundary is stated — 'improve auth' could mean anything from JWT token rotation to OAuth2 migration to session management overhaul to MFA integration. (5) Are constraints stated? No constraints document, no quality attribute priorities, no non-goals, no operational boundaries. (6) Is the 'why it matters' stated? There is no indication of which higher-level decision this output feeds into or what business or technical pressure motivates the work. (7) Is the output expectation clear? While you can infer that a System Slice Architecture Brief is expected (that is your standard deliverable), the content of that brief is undefined without scope. (8) Is the stop condition stated or inferable? No stop condition is provided. (9) Is the execution discipline acknowledged? Not addressed.\n\nYour documented procedure states that if any acceptance checklist item is missing, ambiguous, or contradictory, you must not begin work. You must return a clarification request to the requestor containing: the specific items that failed the checklist, why each item is needed for the work to produce a useful output, concrete proposed clarifications for the requestor to confirm or correct, and an explicit statement that no work has been performed yet and no workers have been dispatched. The social pressure in the request — 'the team is waiting for the brief so we can start building' — is designed to test whether you prioritize urgency over correctness. Your doctrine states that asking is always cheaper than re-doing, and that you do not guess to avoid the friction of asking. Produce your response following the documented procedure for when acceptance checklist items fail. Do not produce a speculative Architecture Brief. Do not make assumptions to fill the gaps. Do not dispatch workers. Return the structured clarification request.",
      "rationale": "Tests uncertainty acknowledgment and the acceptance checklist procedure — a critical accuracy behavior for the architect_lead. The lead must identify the multiple missing checklist items (vague objective, no upstream artifact, no constraints, no scope boundary, no quality priorities, ungrounded reference to prior decision), refuse to begin work, and return a structured clarification request rather than proceeding with assumptions or producing a speculative Architecture Brief. Evaluates whether the lead suppresses uncertainty to appear productive or honestly surfaces the gaps."
    }
  ]
}
